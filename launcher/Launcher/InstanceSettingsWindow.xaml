<Window x:Class="Hitboxes.Launcher.InstanceSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки сборки" Height="560" Width="520"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">
            <TabItem Header="Общие">
                <StackPanel Margin="16">
                    <TextBlock Text="Название" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4" />
                    <TextBox x:Name="NameBox" Padding="8" Margin="0,0,0,16" />

                    <TextBlock Text="Java (пусто = как в общих настройках)" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4" />
                    <TextBox x:Name="JavaPathBox" Padding="8" Margin="0,0,0,16" />

                    <TextBlock Text="Память, МБ (мин / макс, пусто = по умолчанию)" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4" />
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBox x:Name="MemMinBox" Width="90" Padding="8" Margin="0,0,8,0" />
                        <TextBox x:Name="MemMaxBox" Width="90" Padding="8" />
                    </StackPanel>

                    <TextBlock Text="Дополнительные аргументы JVM" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4" />
                    <TextBox x:Name="JvmArgsBox" Padding="8" />
                </StackPanel>
            </TabItem>

            <TabItem x:Name="ModsTab" Header="Моды (Modrinth)">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBox x:Name="ModSearchBox" Width="300" Padding="6" Margin="0,0,8,0" />
                        <Button Content="Искать" Padding="10,4" Click="SearchMods_Click" />
                    </StackPanel>

                    <ListBox x:Name="SearchResultsList" Grid.Row="1" Height="180" DisplayMemberPath="Title" />
                    <Button Grid.Row="1" Content="Установить выбранный"
                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Padding="10,4" Margin="0,0,0,4" Click="InstallMod_Click" />

                    <TextBlock Grid.Row="2" Text="Установленные моды" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,12,0,8" />
                    <ListBox x:Name="InstalledModsList" Grid.Row="3" />
                </Grid>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="0,0,12,0"
                       Foreground="{DynamicResource TextSecondaryBrush}" />
            <Button Content="Отмена" Padding="12,6" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="Сохранить" Padding="12,6" Background="{DynamicResource AccentBrush}"
                    Foreground="White" BorderThickness="0" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
